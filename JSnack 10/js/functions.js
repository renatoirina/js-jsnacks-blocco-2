/**
 * Conta il numero di elementi presenti nell'array.
 * @param {Array} array - L'array di elementi da contare.
 * @returns {number} - Il numero di elementi nell'array.
 */
function countElements(array) {
    return array.length;
}
