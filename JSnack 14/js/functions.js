/**
 * Restituisce un nuovo array con tutti gli elementi dell'array originale, tranne il primo.
 * @param {Array} array - L'array di elementi da cui rimuovere il primo elemento.
 * @returns {Array} - Il nuovo array senza il primo elemento.
 */
function rimuoviDallaTesta(array) {
    return array.slice(1);
}
