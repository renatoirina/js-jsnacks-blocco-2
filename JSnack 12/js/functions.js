/**
 * Restituisce una stringa contenente tutti gli elementi dell'array, separati da una virgola.
 * @param {Array} array - L'array di elementi da stampare.
 * @returns {string} - La stringa contenente gli elementi dell'array separati da virgola.
 */
function stampa(array) {
    return array.join(",");
}
